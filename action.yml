name: "Create Daily plan Issue"
description: "automatic create daily plan issue for Leiloloaa/daily-plan"
inputs:
  token:
    description: 'github token'
    required: true
runs:
  using: 'node12'
  main: 'dist/index.js'
